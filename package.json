{
  "name": "aiagent-club",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "db:init": "sqlite3 data.db < db/schema.sql",
    "collect": "node --env-file=.env src/run.mjs",
    "r2": "node --env-file=.env src/r2.mjs",
    "report": "node --env-file=.env src/report.mjs",
    "test": "node --test"
  }
}
